class Solution:
    def checkEqual(self, a, b) -> bool:
        if len(a)!=len(b):
            return False
        elif sorted(a)==sorted(b):
            return True
        else:
            return False
