def sum():
    t=int(input())
    i=0
    while i < t:
        a,b,c=map(int,input().split())
        if a+b==c or b+c==a or c+a==b:
          print("YES") 
        else:
          print("NO") 
        i+=1
sum()
